# SnapshotScreener — 방법론 확장 문서

> 작성일: 2026-03-25  
> 상태: 초안 / 파일럿 진행 시 지속 업데이트 필요  
> 선행 문서: 대표 스냅샷 자동 추출기 (설계), 대표 스냅샷 분석방법 리뷰 (1차 리뷰)  
> 도구명: **SnapshotScreener** (CLI: `snapshot-screener`, 패키지: `snapshot_screener`)

---

## 1. 문서 목적

1차 설계 문서는 "Vision AI 입력 후보를 줄이는 필터"로서의 추출기를 정의했다. 이후 2차 리뷰와 논의를 거치며 활용 범위가 확장되었고, 파라미터 튜닝에 대한 구체적 방법론이 추가되었다. 이 문서는 그 확장된 방법론을 별도로 정리하여, 파일럿 진행 시 참조·업데이트할 수 있도록 한다.

---

## 2. SnapshotScreener의 3가지 활용 레이어

원래 설계에서는 단일 목적(Vision 입력 축소)이었으나, 논의를 통해 3개 레이어로 확장되었다.

### 레이어 A — Vision AI 입력 축소 (원래 설계)

대량 스냅샷에서 대표 프레임만 추출하여 Vision 처리량을 줄인다. 설계 문서의 핵심 기능이며, 변경 없음.

### 레이어 B — 패턴 가능성 사전 스크리너

1만 대 장비 전체에 Vision AI를 투입하기 전에, fname 메타데이터 + pHash만으로 "이 장비에 자동화 가능한 반복 패턴이 존재할 가능성"을 사전 판정한다.

**활용 시나리오:**

```
전체 1만 대 장비
    ↓
[SnapshotScreener] ← 레이어 B (이미지 거의 안 읽음, fname + pHash 위주)
    ↓
패턴 가능성 높음 ~수백 대  |  낮음 ~나머지
    ↓
Vision AI 집중 투입 (레이어 A 적용)
```

**사전 스크리닝에 사용 가능한 신호:**

| 신호 | 측정 방법 | 패턴 가능성 높음 | 낮음 |
|------|-----------|-----------------|------|
| 클릭 좌표 집중도 | clustered clicks / total clicks | > 70% | < 40% |
| 세션 길이 변동계수(CV) | std(session_lengths) / mean(session_lengths) | < 0.3 | > 1.0 |
| 세션 간 화면 시퀀스 유사도 | 아래 3절에서 별도 정의 | 높음 | 낮음 |

**판정 방향 원칙 — False Negative 최소화:**

- 패턴 있는 장비를 "없음"으로 판정 → Vision 기회 자체를 놓침 (치명적)
- 패턴 없는 장비를 "있음"으로 판정 → Vision 낭비이나 허용 가능
- 따라서 초기에는 임계값을 낮게 설정하여 더 많이 통과시키고, 파일럿 결과와 대조하며 점진적으로 조정

**한계 — 이 레이어로 판단할 수 없는 것:**

- 화면이 반복되더라도 의미 있는 업무 패턴인지 여부
- 같은 좌표 클릭이 실제로 같은 UI 요소인지 여부 (화면이 바뀌었을 수도 있음)
- 화면은 동일하지만 입력값만 다른 반복 업무 (pHash로 구분 불가)
- 화면이 자주 바뀌지만 실제로는 무의미한 탐색 (오탐 가능)

### 레이어 C — 파라미터 민감도 기반 신뢰도 판정

레이어 B의 단일 설정 판정을 보강한다. 핵심 파라미터(주로 PHASH_SIMILAR_THRESHOLD)를 범위 내에서 sweep하면서, 결과의 안정성 자체를 메타 신호로 활용한다.

**핵심 아이디어:** "파라미터를 바꿔도 결과가 안 흔들리는 장비"는 구조가 명확하다.

```
PHASH_SIMILAR_THRESHOLD = [3, 4, 5, 6] 으로 sweep
    ↓
각 설정에서 대표 프레임 fname 집합 산출
    ↓
설정 간 Jaccard 유사도 계산
    ↓
유사도 높음 → 파라미터에 둔감 → 구조 명확 → 패턴 신뢰도 높음
유사도 낮음 → 파라미터에 민감 → 구조 모호 → 판정 보류 또는 추가 분석 필요
```

**기대 효과:** 레이어 B의 이진 판정(있다/없다)에 신뢰도 등급을 부여할 수 있다. 예를 들어:

| 민감도 | 판정 |
|--------|------|
| Jaccard > 0.8 (둔감) | 높은 신뢰도로 패턴 존재 판정 |
| Jaccard 0.5~0.8 | 중간 신뢰도, Vision 투입 시 우선순위 중위 |
| Jaccard < 0.5 (민감) | 판정 보류, 추가 데이터 수집 또는 수동 검토 |

**검증 필요 사항:** 민감도 분포가 실제로 bimodal(명확 vs 모호가 구분됨)하게 나오는지를 파일럿 초기에 반드시 확인해야 한다. bimodal하지 않으면 이 접근의 변별력이 부족하다는 의미이므로 다른 신호를 추가하거나 접근을 재설계해야 한다.

---

## 3. 미정의 항목 — 세션 간 화면 시퀀스 유사도 측정

레이어 B의 핵심 신호이나, 1차 설계 문서와 리뷰 모두에서 구체적 측정 방법이 빠져 있다.

### 후보 방법론 비교

| 방법 | 특성 | 장점 | 단점 |
|------|------|------|------|
| Levenshtein (편집 거리) | 삽입/삭제/치환 횟수 | 직관적 | 부분 생략에 엄격 |
| Jaccard (집합 유사도) | 순서 무시, 포함 여부만 | 계산 간단 | 순서 정보 소실 |
| LCS (최장 공통 부분수열) | 순서 보존, 부분 생략 허용 | 중간 단계 생략에 관대 | 계산량 다소 높음 |

### 권장: LCS 기반 유사도

```
유사도 = LCS 길이 / max(시퀀스A 길이, 시퀀스B 길이)
```

**선택 근거:** 공장 업무에서 A→B→C→D 순서가 반복되더라도, 특정 상황에서 A→B→D(C 생략)로 수행되는 경우가 있을 수 있다. LCS는 이런 부분 생략에 관대하면서도 순서를 보존하므로 공장 패턴에 적합하다.

**파일럿에서 확정할 사항:** 위 세 방법을 같은 데이터에 적용해보고, 사람의 직관적 판단("이 두 세션은 같은 업무다")과 가장 일치하는 방법을 선택한다.

---

## 4. 2차 리뷰에서 도출된 구현 보완 사항

### 4-1. pHash 계산 위치

| 위치 | 장점 | 단점 |
|------|------|------|
| 장비 PC Agent | 중앙 부하 감소 | Agent 무거워짐, 장비 PC 스펙 제약 |
| 중앙 시스템 App (권장) | Agent 경량 유지, 이미 이미지 디코딩 시점 | 중앙 서버 부하 증가 |
| SnapshotScreener 자체 (파일럿) | 기존 시스템 변경 없음 | 첫 실행 시 이미지 전수 읽기 필요 |

**파일럿 결정:** Cassandra 스키마 변경이 어려우므로, SnapshotScreener가 직접 이미지를 읽어 pHash를 계산하고 로컬 캐시(SQLite/CSV)에 저장. 첫 실행 시 전수 읽기, 이후 증분 처리.

### 4-2. DBSCAN eps 해상도 의존 문제

현재 `eps=40`은 절대 픽셀 값으로 하드코딩. 100종 이상의 UI가 해상도/스케일링이 다를 수 있음.

**권장:** 클릭 좌표를 0~1 비율로 정규화한 후 클러스터링. 해상도 메타데이터를 Agent가 함께 전송하거나, 스크린샷 이미지 크기에서 추론.

### 4-3. 세션 분리 복합 기준

현재 시간 gap(15분)만으로 세션을 분리하나, 다음 시나리오에서 오분리 발생:

- 물리적 작업 후 UI 복귀 (10분 gap, 같은 업무) → 세션이 불필요하게 분리됨
- 빠른 교대 인수인계 (5분 내) → 다른 작업자의 세션이 합쳐짐

**권장:** 시간 gap + pHash 상태 변화 복합 기준. 예: gap > 15분이더라도 복귀 후 첫 화면과 이전 마지막 화면의 pHash 거리가 낮으면 같은 세션으로 유지하는 옵션 검토.

### 4-4. 점수 가중치 검증 계획

현재 가중치(session_start: +3, new_screen: +4 등)는 근거 없는 초기값. 파일럿에서 `simple_selector`를 우선 사용하되, Phase 2 전환 시 아래 검증 수행:

- Ablation 테스트: 신호를 하나씩 제거하며 대표 프레임 품질 변화 측정
- Vision AI 인식률과의 상관관계 분석을 통한 가중치 학습

### 4-5. 생산 캘린더 연동

분석 기간 권장안(4주)은 "같은 작업자가 같은 업무"를 전제로 함. 다품종 소량 생산 라인에서는 품목 전환 시 패턴이 달라지므로, 생산 캘린더를 **필수 입력 파라미터**로 승격해야 함.

- 품목 전환 시점을 알면 세션을 품목별로 분리하여 각각 패턴 분석 가능
- 캘린더 연동이 어려우면, 최소한 품목 전환 빈도를 사전 확인하여 분석 기간 조정

### 4-6. False Negative 정량 검증

"임계값을 낮게 잡는다"의 실효성을 검증하려면:

- 파일럿 대상 장비 중 수동으로 "이 장비는 패턴 있음"이라고 레이블한 ground truth 세트 구축
- SnapshotScreener의 recall(패턴 있는 장비를 "있음"으로 맞힌 비율) 측정
- 목표: recall ≥ 95% (precision은 초기에 낮아도 허용)

---

## 5. 기술 스택 및 실행 환경

### 기술 스택

| 기능 | 라이브러리 | 비고 |
|------|-----------|------|
| Cassandra 조회 | `cassandra-driver` | 필수 |
| 시계열 처리/세션 분리 | `pandas` | 필수 |
| pHash 계산/비교 | `Pillow`, `imagehash` | 파일럿: 직접 계산 + 로컬 캐시 |
| 클릭 클러스터링 | `scikit-learn`, `numpy` | DBSCAN |
| 시퀀스 유사도 | `difflib` (표준 라이브러리) | LCS 기반, 외부 의존성 없음 |
| 분포 시각화 | `matplotlib` | 파일럿: 임계값 결정 시 분포 확인용 |
| 리포트 생성 | `jinja2` | HTML 리포트 템플릿 렌더링 |
| pHash 로컬 캐시 | `sqlite3` (표준 라이브러리) | 파일럿: Cassandra 스키마 변경 없이 운용 |

### 실행 환경

- **개발:** 개인 PC (Python 3.10+)
- **실행:** Cassandra 클러스터에 네트워크 접근 가능한 환경 (개인 PC 직접 또는 SSH 경유 서버)
- **배포:** PyInstaller `--onefile`로 단일 exe 생성
- **CLI 실행 예시:**

```
snapshot-screener --eqpid EQ-2471 --from 2026-03-11 --to 2026-03-25 --db-host 10.0.1.50
```

- **출력:** 분석 완료 후 HTML 리포트 파일 자동 생성

---

## 6. 분석 기간 가이드라인

하루 ~3000 스냅샷, 세션 6~15개/일 기준.

| 목적 | 필요 기간 | 근거 |
|------|-----------|------|
| pHash 임계값 보정 | 1~2일 | 분포 측정은 1일치로 충분 |
| 일중 패턴 확인 | 3~5일 | 요일 편차 제거, 평일 최소 3일 |
| 주간 패턴 확인 | 2~3주 | 주 단위 반복 여부 확인 |
| 패턴 유무 판정 (안정적) | 4주 | 변동성 흡수, 이상치 제거 |
| 레이어 C 민감도 분석 | 2주+ | 충분한 세션 수(30+) 확보 필요 |

**전제 조건:** 위 기간은 단일 품목 연속 생산 기준. 품목 전환이 잦은 라인은 기간 연장 또는 품목별 분리 분석 필요. (4-5항 참조)

**실질적 시작 기준:** 2주 데이터로 시작하되, 세션 수가 30개 이상 확보되는지를 먼저 확인. 세션 수가 충분하면 기간을 늘리지 않아도 됨.

---

## 7. 파일럿 검증 체크리스트

파일럿 실행 시 아래 항목을 순차적으로 검증한다.

### 즉시 확정 (파일럿 시작 전)

- [ ] fname 형식 확정 (아키텍처 문서와 추출기 문서 간 불일치 해소)
- [ ] 해상도 정규화 방식 결정 (비율 변환 vs 해상도 메타데이터 전송)
- [ ] 세션 간 화면 시퀀스 유사도 측정 방법 선택 (LCS 권장, 비교 실험 수행)

### 파일럿 초기 (1~2주차)

- [ ] SnapshotScreener 로컬 pHash 캐시 구조 구현 (SQLite)
- [ ] 장비 1대 × 1일치 데이터로 pHash distance 분포 측정
- [ ] 분포의 bimodal 여부 확인 → SIMILAR/TRANSITION 임계값 1차 설정
- [ ] simple_selector로 대표 프레임 추출 → 사람이 육안 검토
- [ ] 생산 캘린더 연동 여부 결정 (품목 전환 빈도 사전 확인)

### 파일럿 중기 (3~4주차)

- [ ] 레이어 C 파라미터 sweep 실행 → 민감도 분포 bimodal 여부 확인
- [ ] ground truth 레이블 세트 구축 (수동, 최소 장비 10대)
- [ ] SnapshotScreener recall 측정 (목표 ≥ 95%)
- [ ] 세션 분리 복합 기준 실험 (시간 gap + pHash 복합)

### 파일럿 종료 시 판정

- [ ] 대표 프레임만으로 업무 흐름 이해 가능 여부 (핵심 질문)
- [ ] 70% 이상 데이터 축소 달성 여부
- [ ] 레이어 B 스크리닝의 실효성 (Vision 투입 대상 축소 비율)
- [ ] 레이어 C 민감도 분석의 변별력 (bimodal 분포 확인)

---

## 8. 종합 판단

SnapshotScreener의 가치는 "패턴 분석의 답을 주는 것"이 아니라 **"Vision AI 전 단계의 비용 절감 도구"**로서 존재한다.

- 이 필터를 통과한 것 ≠ 자동화 가능
- 이 필터에서 걸러진 것 ≠ 자동화 불가
- 핵심 가치: 1만 대 규모에서 Vision 투입 대상을 수백 대로 줄이는 1차 스크리너

파일럿에서 검증해야 할 궁극적 질문: **"SnapshotScreener가 생성한 대표 프레임만 보고, 사람이 업무 흐름을 이해할 수 있는가?"**

이것이 확인되면 SnapshotScreener의 존재 의의가 증명되며, Phase 2 확장의 기반이 된다.

---

*이 문서는 파일럿 진행 상황에 따라 지속 업데이트한다. 각 체크리스트 항목의 결과와 결정 사항을 이 문서에 기록하여 방법론의 이력을 관리한다.*
